<textarea
    rows="{{form-field-textarea-row}}"
    id="{{form-field-textarea-id}}"
    class="{{ styleModifier }}"
    placeholder="{{form-field-textarea-placeholder}}"
    aria-label="{{form-field-textarea-placeholder}}"
    {{# inputRequired }}required{{/ inputRequired}}>
</textarea>
